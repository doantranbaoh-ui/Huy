services:
  - type: worker
    name: nexus-proxy-bot
    runtime: image
    plan: starter
    region: frankfurt
    image:
      dockerfilePath: ./Dockerfile
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ADMIN_USER_IDS
        sync: false
